<div class="row center-xs">
  <div class="col-xs-8 col-md-6">
<% if user_signed_in? %>
    <h1><%= @profe.nombre %></h1>
        <div class="field">
            <%= @profe.rut %>
        </div>
        <div class="field">
            <%= @profe.user.email %>
        </div>
<%end%>